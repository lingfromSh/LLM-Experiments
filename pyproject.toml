[project]
name = "experiments"
version = "0.1.0"
description = "This is a library for llm experiments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "deepeval>=4.1.3",
    "litellm>=1.93.0",
    "datasets>=3.0.0",
    "pyyaml>=6.0",
    "arize-phoenix>=19.18.0",
]
